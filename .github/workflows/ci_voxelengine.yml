name: VoxelEngine CI

# Temporarily only apply to CI branch for testing. This should be replaced with 
# on:
#   push:
#     branches: ['**']
#   pull-request:
#     branches: [master]
on:
  push:
    branches: [ci]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: |
        choco install ninja cmake python visualstudio2022buildtools -y
        choco install llvm --version 14.0.0 -y
        pip install conan
        mkdir out
    - name: run cmake
      working-directory: ./out
      run: |
        vcvarsall.bat
        cmake -DCMAKE_BUILD_TYPE=DEBUG -G Ninja -DVE_GRAPHICS_API=opengl -DENABLE_TESTING=ON -DCMAKE_VERBOSE_MAKEFILE=ON -DCMAKE_C_COMPILER=clang-cl -DCMAKE_CXX_COMPILER=clang-cl ../
    - name: build
      working-directory: ./out
      run: |
        vcvarsall.bat
        cmake --build ./debug --target all
    - name: engine test run
      working-directory: ./out/debug/bin
      run: VELauncher --client --server --run_for=100
    - name: run unit tests
      working-directory: ./out/debug
      run: ctest --extra-verbose
